// lib/models/data_layer.dart
/*
 * We can wrap up our data layer by adding a file that will export both models.
 * That way, our imports will not get too bloated as the app grows.
 * This will only contain export statements instead of actual code.
 */

export 'plan.dart';
export 'task.dart';

